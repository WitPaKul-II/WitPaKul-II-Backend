version: "3.3"
services:
    backend:
        build: .
        image: eiei
        container_name: WitPaKul_Backend
        volumes:
           - ~/Back-End/witpakul-ii-backend/data:/app/data

networks:
    default:
        external:
            name: witpakul_network 
