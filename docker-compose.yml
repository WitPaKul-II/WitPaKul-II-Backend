version: "3.3"
services:
    backend:
        build: .
        image: backend:dev
        volumes:
           - ./pictures:/pictures
networks:
    default:
        external:
            name: witpakul_network 