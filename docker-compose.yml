version: "3.3"
services:
    backend:
        build: .
        image: backend
        container_name: WitPaKul_Backend
        volumes:
           - ./data:/app/data/

networks:
    default:
        external:
            name: witpakul_network 
